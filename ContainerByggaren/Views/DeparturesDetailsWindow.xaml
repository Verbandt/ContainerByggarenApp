<Window x:Class="ContainerByggaren.Views.DeparturesDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContainerByggaren.Views"
        mc:Ignorable="d"
        Title="Avgångs information" Height="450" Width="800" Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <DataGrid Grid.Row="0" Margin="10,10,10,10" AutoGenerateColumns="False" HeadersVisibility="Column" MinHeight="390" Width="220" VerticalAlignment="Top"
            Background="{DynamicResource PanelBrush}" Foreground="Black" BorderBrush="{DynamicResource BorderBrush}" CanUserAddRows="False" CanUserResizeColumns="False"
            CanUserReorderColumns="False" GridLinesVisibility="All" AlternatingRowBackground="{DynamicResource DatagridAltBg}" FontSize="16" HorizontalAlignment="Left">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Tåg" Binding="{Binding }" Width="Auto"/>
                <DataGridTextColumn Header="Fyllnadsgrad" Binding="{Binding }" Width="Auto"/>
                <DataGridTextColumn Header="Containrar" Binding="{Binding }" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
